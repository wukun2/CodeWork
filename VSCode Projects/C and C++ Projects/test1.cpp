#include<iostream>
#include <cstring>

#include<string.h>
using namespace std;
int main(){
    int dp[300][300];
    for(int i=0;i<300;i++){
        memset(dp[i],0,sizeof(int) * 300);
    }
    cout<<"yes";
}